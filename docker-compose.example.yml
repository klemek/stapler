name: stapler

services:
  stapler:
    build: .
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - "./data:/data"
      - "./letsencrypt:/etc/letsencrypt"
    environment:
      - HOST=${HOST}
      - TOKEN_SALT=${TOKEN_SALT}
    command: run
